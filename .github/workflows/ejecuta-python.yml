name: Prueba ejecucion de Script
run-name: Script de Python en Ubuntu
on : [push]
jobs:
 ejecuta-script-python:
  runs-on: ubuntu-latest
  steps:
    -run: echo "Inicia Actions"
    -run: python --version 
    -name: Check out repositiry code
      uses: actions/checkout@v6
    -run: ls -al
    -run: python app.py